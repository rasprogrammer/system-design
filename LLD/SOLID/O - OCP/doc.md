/*
CP does not mean:
- "After writing a class, never touch it again."

It means:
- When adding a new type of behavior, design the code so existing stable code doesn't need to be modified unnecessarily.


Where do we mostly use OCP?
- Payment systems
- Authentication
- Notification systems
- File/storage systems
- Export/report systems
- Database/repository implementations
*/